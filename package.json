{
  "name": "@jw-rami/worktree-audit",
  "version": "0.1.0",
  "description": "Interactive, fail-closed Git worktree cleanup assistant.",
  "private": true,
  "type": "module",
  "bin": {
    "worktree-audit": "./src/cli.mjs"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/audit.mjs && node --check src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
