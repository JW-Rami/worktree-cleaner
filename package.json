{
  "name": "@jw-rami/worktree-audit",
  "version": "0.2.0",
  "description": "Interactive, fail-closed Git worktree cleanup assistant.",
  "private": true,
  "type": "module",
  "bin": {
    "worktree-audit": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/cli.js",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2"
  }
}
